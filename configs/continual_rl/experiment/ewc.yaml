# @package _global_
defaults:
- override /policy: ewc
- override /task_seq: ???
- _self_

name_prefix: ${task_seq.name}-ewc-dqn_
name: ${call_module:crl.policies.ewc.ewc_dqn_policy_config.experiment_tag,${policy},${task_seq}}

task_seq:
  num_timesteps: 300000

policy:
  policy_kwargs:
    buffer_size: 50000
    fisher_lambda: 100000
